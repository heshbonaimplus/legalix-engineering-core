# ארכיטקטורת אבטחה, הרשאות לקוחות וחיבורי MCP — איך הלקוחות מתחברים בבטחה

---

### 🛡️ 1. ההבדל בין חיבור ה-Admin שלך לבין חיבור לקוחות קצה:

1. **החיבור שלך כרגע (Master Admin Connection):**
   - אתה מגדיר את השרת בחשבון הניהול שלך ב-Claude עם `No sign-in` כדי שתהיה לך גישת-על מיידית, חלקה וללא חסימות.
   - הכתובת היא שלך ורק אתה מנהל אותה.

2. **איך לקוחות מתחברים ואיך הם מאומתים (Client Access & Licensing):**
   - לקוחות קצה **לא מתעסקים עם הגדרות טכניות ומחברים מותאמים אישית**.
   - לקוחות משתמשים במערכת ב-2 דרכים פשוטות, מאובטחות וסגורות:

---

### 🔑 2. שתי הדרכים שבהן לקוחות משלמים ומתחברים:

#### מסלול א': מפתח רישיון ייחודי לכל לקוח (Client API Key / Tenant Token)
* לכל לקוח (למשל משרד עורכי דין) יש מפתח רישיון ייחודי שהוא מקבל ממך (למשל: `LEGALIX_CLIENT_AMRAM_9042`).
* כאשר הלקוח פונה ל-MCP דרך האפליקציה או התוסף — השרת בודק:
  - האם המפתח בתוקף?
  - לאיזה תיקים בדרייב מותר ללקוח הזה לגשת?
* **אם הלקוח לא שילם:** המפתח שלו נחסם מיידית בשרת, והוא לא יכול לגשת לשום מידע!

#### מסלול ב': ממשק ה-Custom GPT / Claude Projects הממותג (Zero-Friction Client UI)
* הלקוח מקבל קישור ייעודי לפרויקט סגור וממותג.
* החיבור לשרת נעשה מאחורי הקלעים דרך שרת הליבה המאובטח שלנו, ללא שום חשיפה של השרתים או הנתונים של לקוחות אחרים!

---

### 🏆 למה לסמן `No sign-in` במסך הנוכחי שלך:
* במסך הזה Claude שואל אם יש שרת OAuth חיצוני שמבקש שם משתמש וסיסמה של גוגל/מייקרוסופט.
* בחירה ב-`No sign-in` במסך זה **מאפשרת לך לחבר את השרת ישירות לחשבון שלך**, כאשר מנגנון הרישיונות והאבטחה של הלקוחות מנוהל בצורה מקצועית ישירות בתוך שרת הליבה של Legalix!
EOF
