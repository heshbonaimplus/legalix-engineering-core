# מיפוי מושלם — חיבור ארכיטקטורת 13 הסוכנים ישירות ל-38 כלי ה-MCP הקיימים של Legalix AI
## אינטגרציה מלאה: משפחת legalix_case + משפחת legalix_kepa_* + סוכני OpenClaw

---

### 🏛️ 1. פתרון התעלומה מול קלוד:
קלוד רואה בדיוק את **38 הכלים המובנים של Legalix AI** ב-MCP:
1. **משפחת `legalix_case`:** פתיחת תיקים, לקוחות, העלאת קבצים, OCR וחיפוש עמוק (`read_pages`, `overview`).
2. **משפחת `legalix_kepa_*` (מחסן העובדות והסוכנים):**
   - `legalix_kepa_status` — סטטוס מחסן העובדות.
   - `legalix_kepa_brief` — תמצית התיק.
   - `legalix_kepa_entities` — סוכן הישויות (01).
   - `legalix_kepa_timeline` — סוכן ציר הזמן (03).
   - `legalix_kepa_search_facts` — סוכן הסתירות והעובדות (01 + 04).
   - `legalix_kepa_get_chunk` — מראי מקום אטומיים לכל שורה.
3. **משפחת הניסוח והפרוטוקולים:**
   - `legalix_draft_document` — הפקת כתבי טענות.
   - `legalix_analistim_protocol` & `legalix_kaftorim_protocol`.

---

### 🎯 2. ההתאמה המדויקת של 13 המחסנים לתוך כלי ה-MCP הקיימים:

| סוכן / מחסן בחדר המלחמה | כלי ה-MCP הקיים ב-Legalix AI |
|---|---|
| **01. עובדות וטענות** | `legalix_kepa_entities` + `legalix_kepa_search_facts` |
| **02. כספים ופורנזיקה** | `legalix_tax_*` + `legalix_case` search |
| **03. ציר זמן כרונולוגי** | `legalix_kepa_timeline` |
| **04. סתירות ושקרים** | `legalix_kepa_search_facts` + `legalix_analistim_protocol` |
| **05. ראיות ומוצגים** | `legalix_case` (`read_pages`, `overview`) |
| **08. דין ופסיקה** | `legalix_tax_*` + מאגר הפסיקה המובנה |
| **11. כתבי טענות** | `legalix_draft_document` + `legalix_kaftorim_protocol` |
EOF
