# מיפוי שרתי ה-MCP ב-Claude Desktop ובשרת הענן — איפה יושב שרת ה-Mega-Case

---

### 🌐 1. איפה יושב ה-MCP בפועל (הארכיטקטורה הפיזית):

שרת ה-MCP שקלוד מתחבר אליו יושב ב-**שרת הענן המרכזי של Legalix ב-Google Cloud (`legalix-master-server` בכתובת `35.242.250.144`)**.

---

### 🔌 2. שני שרתי ה-MCP שמוגדרים ב-Claude Desktop שלך:

בקובץ ההגדרות של קלוד במחשב (`%APPDATA%\Claude\claude_desktop_config.json`):

1. **שרת ה-Mega-Case & Litigation (Legalix AI - 38 כלים):**
   * זהו השרת שמחזיק את `legalix_case`, את משפחת `legalix_kepa_*` (13 מחסני הסוכנים), את מנוע ה-OCR והתמלול, ואת הסקילים של הפקת כתבי הטענות (`legalix_draft_document`).
   * קלוד פונה אליו דרך ה-MCP ומקבל גישה לכל התיקים והמסמכים!
2. **שרת ה-Engineering & TaxLand (Legalix Master Cloud):**
   * שרת ה-MCP שמחזיק את מנועי ההנדסה (14 יועצים, OpenSees, רוויט) ואת מנוע המיסוי של TaxLand (ליניארי מוטב, 49ז).

---

### 💻 3. איך קלוד מנהל הכל מחלון אחד (The Unified Hub):
כאשר אתה מדבר עם קלוד בדסקטופ:
* קלוד רואה את שני השרתים בסרגל הכלים שלו (סמל הפטיש 🔨).
* כשאתה אומר לו: *"תפתח תיק משפטי / תנתח סתירות / תבצע OCR"* ➔ קלוד מפעיל את כלי ה-Mega-Case של Legalix AI.
* כשאתה אומר לו: *"תבדוק קונסטרוקציה / תחשב מס שבח"* ➔ קלוד מפעיל את כלי ה-Engineering ו-TaxLand.
EOF
