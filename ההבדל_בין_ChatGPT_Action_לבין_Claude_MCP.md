# ההבדל הטכנולוגי בין ChatGPT Actions לבין Claude MCP — איך קלוד יודע מה לעשות
## השוואה ארכיטקטונית: Schema ידנית מול פרוטוקול MCP אוטונומי

---

### 🔍 1. איך זה עובד ב-ChatGPT (שיטת ה-Action הידנית):
* ב-ChatGPT היית צריך להיכנס ידנית ל-`Create new action`, לפתוח תיבת טקסט ולהדביק בתוכה קוד JSON ארוך שנקרא **OpenAPI Schema**.
* ה-Schema הסבירה ל-ChatGPT אילו פונקציות קיימות, מה הפרמטרים ולאיזה URL לפנות.

---

### ⚡ 2. איך זה עובד ב-Claude (שיטת ה-MCP המתקדמת והאוטומטית):
ב-Claude משתמשים בפרוטוקול העולמי החדש שנקרא **MCP (Model Context Protocol)**.
בשיטה הזו:
* **אתה לא צריך להדביק שום קוד ושום סכימה ידנית!**
* ברגע שהזנת את כתובת ה-MCP של השרת (`https://.../mcp`), מתרחש תהליך שנקרא **Self-Discovery (גילוי עצמי אוטומטי)**:

```text
┌────────────────────────────────────────────────────────────────────────┐
│ 1. קלוד שולח לשרת הענן פקודת לחיצת יד: `tools/list`                    │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ ⚡
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 2. שרת ה-MCP בענן של Legalix מחזיר לקלוד את כל הסכימות באופן אוטומטי:   │
│    • מה שם הכלי (`legalix_mega_case`).                                │
│    • מה התפקיד שלו (ניתוח תיקי ענק, איתור סתירות בבורות גישוש).        │
│    • אילו שדות נדרשים (`case_id`, `query`, `task`).                    │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ 🧠
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 3. קלוד לומד את הכלים ומבין בדיוק מוחלט מה לעשות — באפס מאמץ מצדך!    │
└────────────────────────────────────────────────────────────────────────┘
```

---

### 🏆 למה שיטת ה-MCP של Claude עדיפה בהרבה:
1. **אפס טעויות הדבקה:** לא צריך להתעסק עם קוד JSON, סוגריים מסולסלים או שגיאות תחביר.
2. **עדכונים חיים אוטומטיים:** כשאני מוסיף כלי חדש בשרת הענן — קלוד מקבל אותו מיד בלי שתצטרך לעדכן שום סכימה!
EOF
