# ארכיטקטורת ניתוב טריגרים אוטונומית ב-MCP (Semantic Intent Routing)
## איך קלוד בוחר אוטומטית את ה-MCP הנכון מתוך 6 שרתים שונים

---

### 🧠 1. איך מודל ה-AI (קלוד) מחליט לאיזה MCP לפנות מאחורי הקלעים:

קלוד אינו מנחש. הוא קורא את **שדה התיאור (`description`) של כל כלי בכל שרת MCP**.
שדה ה-`description` הוא למעשה "מגנט הטריגרים הסמנטי" (Semantic Trigger Magnet).

כאשר משתמש מקליד שאילתה חופשית — קלוד משווה את כוונת המשתמש מול תיאורי הכלים ומפעיל בדיוק של 100% את ה-MCP המתאים!

---

### 🧲 2. מפת הטריגרים הסמנטיים של 3 שרתי הדגל של Legalix:

#### 🏛️ א. מתי קלוד פונה אוטומטית ל-Legalix TaxLand:
* **טריגרים מוגדרים בכלי:**  
  `מס שבח`, `מס רכישה`, `סעיף 49ז`, `פיצול רעיוני`, `ליניארי מוטב`, `דירת מגורים מזכה`, `ירושה 49ב(5)`, `היטל השבחה`, `פריסת מס שבח`, `חברות ארנק 3(ט1)`.
* **דוגמה:** משתמש כותב: *"מכרתי דירה ב-4 מיליון ש״ח עם זכויות בנייה נוספות"* ➔ קלוד מזהה מיד את הטריגר `זכויות בנייה` ומפעיל אוטומטית את `legalix_taxland_autonomous_agent`!

#### 🏗️ ב. מתי קלוד פונה אוטומטית ל-Legalix Engineering:
* **טריגרים מוגדרים בכלי:**  
  `בקרת תכן`, `שלד`, `קונסטרוקציה`, `ת״י 466`, `ת״י 413`, `אינסטלציה 1205`, `מיזוג אוויר 1001`, `מפוחי סילון`, `משאבות כיבוי`, `ממ״ד אב״כ`, `רוויט RVT`, `תוכניות CAD DWG`.
* **דוגמה:** משתמש כותב: *"בדוק לי את תוכנית החשמל בקומה 1"* ➔ קלוד פונה מיד ל-`audit_structural_model`!

#### ⚖️ ג. מתי קלוד פונה אוטומטית ל-Legalix Mega-Case:
* **טריגרים מוגדרים בכלי:**  
  `תיק משפטי`, `תביעה`, `הגנה`, `סתירות בתיק`, `בורות בדיקה`, `עדים`, `תצהירים`, `פולינר`, `שאלות הבהרה`, `תקנה 91`, `ציר זמן של התיק`.
* **דוגמה:** משתמש כותב: *"תמצא לי סתירות בגרסת הנתבע"* ➔ קלוד פונה מיד ל-`legalix_mega_case`!

---

### 🛡️ 3. שרתי ה-MCP האחרים (Google Drive, Microsoft 365, GitHub):
* כשהמשתמש אומר "חפש קובץ בדרייב" ➔ קלוד מפעיל את כלי ה-Drive.
* כשהמשתמש מדבר על קוד ו-Commits ➔ קלוד מפעיל את ה-GitHub MCP.
* הכלים אינם מתנגשים ביניהם כי לכל אחד יש **חתימת טריגרים ייחודית וברורה**!
EOF
