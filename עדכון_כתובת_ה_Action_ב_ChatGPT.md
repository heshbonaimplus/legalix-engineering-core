# עדכון כתובת ה-URL ב-Action של ChatGPT — חיבור השרת הדינמי החדש
## למה ChatGPT פנה לשרת הישן ואיך מחברים אותו לשרת הדינמי ב-30 שניות

---

### 🔍 למה זה קרה:
בשרת הענן החדש שלנו, כששולחים שאילתה על תמונה 22 במיזוג אוויר — השרת מחזיר מיד את גיליון **`M-022` (מפוחי סילון ותעלות עשן)**.
אבל ב-ChatGPT, בתוך לשונית **`Actions`**, ה-URL של השרת היה מוגדר עדיין על כתובת המנהרה הישנה, ולכן ChatGPT קיבל את התשובה הישנה של השלד!

---

### 🛠️ איך לעדכן את ה-URL ב-ChatGPT ב-30 שניות:

1. ב-ChatGPT, היכנס ל-**`Explore GPTs`** ➔ **`My GPTs`** ➔ לחץ **`Edit`** (העיפרון) על הסוכן ההנדסי.
2. עבור ללשונית **`Configure`** ➔ גלול למטה ולחץ על ה-**`Action`** הקיים.
3. בתוך תיבת ה-**`Schema`**, שים לב לשורת ה-**`servers`** בראש הקוד:

החלף את ה-URL לכתובת הפעילה והמעודכנת:
```json
  "servers": [
    {
      "url": "https://inclusion-refer-maintenance-associations.trycloudflare.com"
    }
  ],
```

4. לחץ על **החץ שמאלה ($\leftarrow$)** ➔ ולחץ **`Update` / `Confirm`** בפינה הימנית העליונה!
EOF
