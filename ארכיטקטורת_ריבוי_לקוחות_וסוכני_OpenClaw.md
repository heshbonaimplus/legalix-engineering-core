# ארכיטקטורת ריבוי לקוחות וסוכני OpenClaw — האמת הטכנולוגית המלאה
## איפה יושב המנגנון, איך מתבצע הבידוד בין לקוחות, ואיך מוגדרים 13 סוכני OpenClaw

---

### 🔍 1. מה זה "סוכן OpenClaw" בפועל (ללא מילים באוויר):

סוכן OpenClaw במערכת שלנו אינו "טקסט מופשט" — הוא **יחידת תוכנה אוטונומית (Autonomous Software Worker)** הכוללת 4 מרכיבים קשיחים:

1. **נשמה וגבולות גזרה (`SOUL.md`):** הגדרת תחום המומחיות הספציפי של הסוכן (למשל: סוכן 02 מוגדר לעולם לא לגעת בטענות משפטיות, אלא רק בחשבוניות, ריביות והעברות בנק).
2. **ארגז כלים ייעודי (Dedicated Tools):** כלים ייחודיים שרק אותו סוכן רשאי להפעיל (למשל: סוכן 03 מפעיל מחלץ תאריכים, סוכן 04 מפעיל מנוע הצלבת סתירות).
3. **מחסן זיכרון מבודד (Isolated Vault):** תיקיית ה-JSON הייעודית שלו בדרייב (`01_facts`, `02_financial`, `03_chronology` ... עד `13_remedies`).
4. **לולאת חקירה (Investigation Loop):** הסוכן מקבל מסמך ➔ מחלץ את הנתונים בתחום מומחיותו ➔ וכותב את הרשומה הממוענת לתוך מחסן הזיכרון שלו.

---

### 🖥️ 2. איפה יושב המנגנון הפיזי (חלוקת תפקידים מדויקת):

```text
┌────────────────────────────────────────────────────────────────────────┐
│ 1. שרת הענן המרכזי של Legalix (Google Cloud VM 35.242.250.144):         │
│    • כאן יושב קוד המקור ומנוע ההרצה של OpenClaw:                       │
│      - `legalix_war_room_engine.py` (סוכן-העל המתזמר).                 │
│      - `legalix_megacase_pipeline.py` (מנוע ה-Vision OCR המקבילי).      │
│      - `legalix_taxland_agent_runner.py` (סוכן המיסוי האוטונומי).      │
│    • השרת אינו שומר מידע של תיקים — הוא רק מנוע החישוב והעיבוד!        │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 2. ה-Google Drive הפרטי של כל לקוח (האחסון המבוזר):                    │
│    • כאן יושבים 13 מחסני הסוכנים (קובצי ה-JSON המאונדקסים).            │
│    • כל לקוח וכל תיק מקבלים תיקייה נפרדת לחלוטין:                      │
│      - לקוח א' (עמרם אברהם): `/Drive_A/Cases/Case_101/13_Vaults/`      │
│      - לקוח ב' (משרד עו״ד פולינר): `/Drive_B/Cases/Case_9042/13_Vaults/`│
└────────────────────────────────────────────────────────────────────────┘
```

---

### 🛡️ 3. איך מובטח בידוד מוחלט בין לקוחות שונים (Multi-Tenant Isolation):

כאשר לקוח א' מעלה תיק ולקוח ב' מעלה תיק:
1. **לכל לקוח יש מזהה ייחודי (Tenant Token):**  
   כשלוקח א' שולח שאילתה לקלוד: `case_id: 101` ➔ שרת ה-OpenClaw בענן פונה **אך ורק לתיקיית הדרייב של לקוח א'**.
2. **אפס חשיפה בין תיקים:**  
   סוכן ה-OpenClaw שמטפל בתיק 101 פיזית אינו מסוגל לקרוא, לראות או לדעת על קיומם של קבצים מתיק 202 של לקוח ב'.
3. **סיום תיק ומחיקה:**  
   ברגע שהעבודה על תיק מסתיימת — שרת הענן משחרר את הזיכרון, וכל המידע נשאר שמור ומאובטח אך ורק ב-Google Drive הפרטי של אותו לקוח.
EOF
