# מפת כל הנכסים, המאגרים והתשתיות האוניברסליות של OpenClaw ו-Legalix Engineering בלינוקס
## The Master Infrastructure & Universal Source Map — הכול כולל הכול

---

### 🏛️ 1. ליבת מערכת OpenClaw האוניברסלית בלינוקס (OpenClaw Universal Infrastructure):

1. **סביבת השורש הראשית של OpenClaw (Root Daemon & Environment):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/.openclaw/`  
   * 🪟 **נתיב ישיר ב-Windows (הדבק בסייר הקבצים):**  
     `\\wsl$\Ubuntu\home\yogi\.openclaw\`  
   *(מכיל 156 רכיבי תצורה, דמונים, לוגים וסביבות עבודה).*

2. **מאגר 56 סוכני הליבה האוטונומיים של OpenClaw (The 56 Agents Swarm):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/.openclaw/agents/`  
   * 🪟 **נתיב ב-Windows:**  
     `\\wsl$\Ubuntu\home\yogi\.openclaw\agents\`  
   *(כולל את כל סוכני המומחים: `main`, `gush-helka`, `nidlora`, `ktav-teanot`, `mekorot`, `attacker`, `edut-rishonit`, `tguva`, `sikhum`, `michtavim`, `hagana`, `hozim`, `chai`, `bakasha`, `radar`, `monitor`, `qagate`, `clarify`, `legalix-architect`, `dani`, `amos`, ועוד).*

3. **מאגר הפלייבוקים, החוקים והשלדים של OpenClaw:**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/.openclaw/workspace/legalix-playbooks/`  
   * 🪟 **נתיב ב-Windows:**  
     `\\wsl$\Ubuntu\home\yogi\.openclaw\workspace\legalix-playbooks\`

4. **סביבת העבודה המשפטית הראשית של נידלורה (Nidlora Legal Workspace):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/.openclaw/workspace-nidlora/`  
   * 🪟 **נתיב ב-Windows:**  
     `\\wsl$\Ubuntu\home\yogi\.openclaw\workspace-nidlora\`

---

### 🏗️ 2. ליבת מערכת Legalix Engineering בלינוקס (The Engineering Core Engine):

1. **מאגר הקוד והליבה המלא (Git Repository Core):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/legalix-engineering-repo/`  
   * 🪟 **נתיב ב-Windows:**  
     `\\wsl$\Ubuntu\home\yogi\legalix-engineering-repo\`  
   * 🐙 **מאגר GitHub הרשמי:**  
     👉 **`https://github.com/heshbonaimplus/legalix-engineering-core`**

2. **סביבת העבודה הראשית של מודלי ההנדסה והסולברים (Lod Project Workspace):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/lod_project/`  
   * 🪟 **נתיב ב-Windows:**  
     `\\wsl$\Ubuntu\home\yogi\lod_project\`  
   *(מכיל 468 קבצים: מודלי Revit RVT מקוריים, שרטוטי CAD DWG, מנועי הפיזיקה של OpenSees, סקריפטי חילוץ כמויות, ו-50 גיליונות שרטוט מרונדרים).*

3. **ספריות חוקי התכן הישראליים ב-JSON (Rules Libraries):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/legalix-engineering-repo/rules_libraries/`  
   * 🪟 **נתיב ב-Windows:**  
     `\\wsl$\Ubuntu\home\yogi\legalix-engineering-repo\rules_libraries\`  
   *(כולל את `PLUMBING_RULE_LIBRARY.json`, `HVAC_RULE_LIBRARY.json`, `STRUCTURAL_RULE_LIBRARY.json`, ו-`ELECTRICAL_RULE_LIBRARY.json`).*

4. **מנוע הפיזיקה והאלמנטים הסופיים הראשי (OpenSees FEA Engine):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/lod_project/legalix_physics_engine_master.py`

5. **מאגר התמונות והשרטוטים ברזולוציה גבוהה (Extracted High-Res Drawings):**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/lod_project/extracted_doc_images/`

---

### ⚖️ 3. מחסני הליטיגציה של Mega-Case בלינוקס ובענן:

1. **13 מחסני הסוכנים המאונדקסים בדיסק בלינוקס:**  
   * 🐧 **נתיב בלינוקס:** `/home/yogi/lod_project/poliner_war_room_vault/`  
   * 🪟 **נתיב ב-Windows:**  
     `\\wsl$\Ubuntu\home\yogi\lod_project\poliner_war_room_vault\`
2. **13 מחסני הסוכנים ב-Google Drive:**  
   * ☁️ **נתיב בענן:** `Google Drive > תיקים משפטיים לפני OCR > פולינר > חדר_מלחמה_13_סוכנים/`

---

### 🌐 4. שערי ה-Production בענן (GCP & Cloud Run):

* 🔗 **שער ה-MCP הרשמי לקלוד ו-ChatGPT:**  
  👉 **`https://mcp.legalix.ai/mcp`**
* 🔗 **שרת הענן המרכזי של גוגל (GCP Linux VM):**  
  👉 **`35.242.250.144`** (במרכז הנתונים פרנקפורט)
* 🔗 **מנהרת הפיתוח החיה (Dev Tunnel):**  
  👉 **`https://inclusion-refer-maintenance-associations.trycloudflare.com/audit-structural-model`**

---

### 💻 5. תיקיית העבודה המקומית המלאה ב-Windows (Desktop):

* 📁 **נתיב ישיר במחשב שלך:**  
  👉 **`C:\Users\user1\Desktop\פרויקט_לוד_קונסטרוקציה\`**  
  *(מכיל 277 קבצים מלאים: כל דוחות ה-Word, ה-PDF, השרטוטים, חוקות העבודה, והמפרטים המלאים).*
EOF
