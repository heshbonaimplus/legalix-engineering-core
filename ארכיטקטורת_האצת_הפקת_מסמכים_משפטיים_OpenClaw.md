# ארכיטקטורת חדר המלחמה האוטונומי — החלפת סוכן ה-Legacy בסוכן OpenClaw והאצת ה-Pipeline
# מיפוי צווארי הבקבוק, נקודת החיבור המדויקת, וזרימת העבודה החדשה ב-Legalix

---

### 🔍 1. למה בשיטה הנוכחית לוקח המון זמן עד שמופק מסמך משפטי (3 צווארי בקבוק):

1. **עיבוד טורי איטי (Sequential Bottleneck):**  
   כרגע המערכת מבצעת OCR, מחכה שכל 10,000 המסמכים יסתיימו, ואז סוכן אחד בודד ואיטי מנסה לקרוא הכל בבת אחת.
2. **סוכן ישן ללא כושר ביצוע אוטונומי (Underpowered Legacy Agent):**  
   הסוכן הקיים רק "קורא טקסט" אבל אין לו יכולת לחלק משימות, להצליב נתונים במקביל, או להריץ סקריפטים.
3. **עומס עצום על ה-Prompt:**  
   סוכן יחיד שמנסה לבדוק גם כספים, גם סתירות, גם פסיקה וגם לנסח כתב טענות נחנק ממגבלת הזיכרון.

---

### 🚀 2. איפה בדיוק נכנס סוכן OpenClaw ואיך הוא משנה את התמונה (The Parallel Swarm Pipeline):

```text
┌────────────────────────────────────────────────────────────────────────┐
│ שלב 1: פתיחת תיק מקלוד דרך ה-MCP (בלחיצה אחת):                        │
│    • הלקוח בקלוד כותב: "פתח לי תיק חדש בלגליקס: תביעת כהן נ' לוי"     │
│    • קלוד פונה ל-MCP ➔ שרת הענן של Legalix פותח תיקייה ומחזיר קישור.  │
│    • הלקוח מעלה את כל המסמכים, הסריקות, ההקלטות והמיילים.             │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ שלב 2: מנוע ה-Multimodal OCR המקבילי של Legalix (מהיר פי 20!):         │
│    • במקום לחכות — יוגי מריץ עיבוד מקבילי (Batch Vision / Whisper).    │
│    • כל עמוד מקבל תעודת זהות וטקסט מפוענח בתוך 2 שניות!                │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ שלב 3: כניסת 13 סוכני OpenClaw — חלוקת עבודה מקבילית בזמן אמת!         │
│    ברגע שמסמך מסיים OCR, הוא מוזרם ישירות ל-13 הסוכנים בו-זמנית:       │
│    • סוכן הכספים (02) שואב מיד את החשבוניות וההעברות.                 │
│    • סוכן ציר הזמן (03) נועץ את התאריך בציר האירועים.                 │
│    • סוכן הסתירות (04) מצליב מול מסמכים קודמים ומאתר סתירות בלייב!    │
│    • סוכן הראיות (05) מקטלג את המסמך כמוצג רשמי.                       │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ שלב 4: הפעלת הסקילים המשפטיים והפקת המסמך הסופי (Instant Assembly):    │
│    • הסוכן הראשי (Lead Orchestrator) לוקח את החומרים מכל הסוכנים.      │
│    • מפעיל את הסקיל המשפטי המתאים (כתב תביעה, הגנה, מכתב התראה, בקשה) │
│    • מפיק חוברת דוח וכתב טענות מושלם ב-Word (DOCX) ב-100% RTL תוך דקות!│
└────────────────────────────────────────────────────────────────────────┘
```

---

### 📍 3. איפה בדיוק יושבים הסוכנים בתוך המערכת (החלפת הסוכן הישן ב-OpenClaw):

במקום הסוכן הישן והאיטי, אנחנו מגדירים בשרת הענן של Legalix (`legalix-master-server` בכתובת `35.242.250.144`):

1. **נתיב הסוכנים בשרת:**  
   📂 `/opt/legalix/war_room_agents/`
   * `00_lead_orchestrator.py` — המנצח הראשי.
   * `01_fact_claims_agent.py` ... `13_damages_agent.py` — 13 הסוכנים המומחים.
2. **חיבור ישיר ל-MCP:**  
   ה-MCP Server של Legalix חושף לקלוד כלי אחד חזק ומהיר:  
   👉 `legalix_war_room_investigate(case_id, query, task)`
3. **אינטגרציה לסקילים המשפטיים הקיימים:**  
   הסוכנים שואבים את המידע ומזינים אותו ישירות לתוך הסקילים המשפטיים שכבר בנית (`ktav-teanot`, `michtavim`, `bakasha`, `tovea`), כך שרמת הניסוח המשפטית נשארת בסטנדרט הגבוה ביותר!

---

### ⚡ למה השיטה החדשה תהיה מהירה פי 10:
* **במקום לעבוד בטור (אחד אחרי השני):** 13 הסוכנים מעבדים את התיק במקביל תוך כדי ה-OCR!
* **ברגע שה-OCR מסתיים:** כל ציר הזמן, טבלת הסתירות, הראיות וחשבון הפיצויים **כבר מוכנים**, וכתב הטענות מופק תוך 30 שניות!
EOF
