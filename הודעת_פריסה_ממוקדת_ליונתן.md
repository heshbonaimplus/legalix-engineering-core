# הודעת משימה ברורה וממוקדת ליונתן — פריסת 3 הכלים החדשים ב-Cloud Run של mcp.legalix.ai

היי יונתן,

אנחנו צריכים להעלות גרסה מעודכנת ל-Cloud Run של ה-MCP הקבוע שלנו (`https://mcp.legalix.ai/mcp`) כדי לחבר בלייב 3 כלים חדשים שפיתחנו (Mega-Case, TaxLand ו-Engineering).

הכנתי וארזתי את כל קוד המקור המעודכן, הבדיקות והספריות בתוך קובץ ארכיב מוכן ב-Google Drive.

---

### 📦 מיקום החבילה המוכנה לפריסה:
📂 **Google Drive:**  
`Legalix MCP Gateway / legalix-mcp-full-megacase-taxland-engineering.tar.gz`

---

### 🛠️ 3 הכלים החדשים שמוטמעים בחבילה (בנוסף ל-38 הכלים הקיימים ב-tools.ts):
1. ⚖️ **`legalix_mega_case`** — חדר מלחמה, ניתוח תיקי ענק מולטי-מודליים, איתור סתירות וציר זמן מעוגן.
2. 🏛️ **`legalix_taxland_autonomous_agent`** — סוכן תכנון מס מקרקעין רב-מסלולי (ליניארי מוטב 48א, פיצול 49ז ופריסת מס).
3. 🏗️ **`audit_structural_model`** — בקרת תכן רב-תחומית ל-14 היועצים ההנדסיים (שלד, מיזוג, אינסטלציה, חשמל ופיתוח).

---

### 🚀 פקודת הפריסה להרצה ב-Google Cloud Shell / CLI:

```bash
# 1. חילוץ החבילה המעודכנת
tar -xzf legalix-mcp-full-megacase-taxland-engineering.tar.gz
cd legalix_mcp_prod_source

# 2. הרצת Cloud Build & Deploy ל-Cloud Run
gcloud builds submit --config cloudbuild.yaml \
  --substitutions=_REGION=me-west1,_SERVICE=legalix-mcp
```

כל הקוד נבדק בטסטים פנימיים ונארז מוכן לפריסה חלקה.  
תעדכן אותי בבקשה ברגע שה-Deploy מסתיים כדי שאוודא שהכלים עולים ב-`tools/list`. תודה!
EOF
