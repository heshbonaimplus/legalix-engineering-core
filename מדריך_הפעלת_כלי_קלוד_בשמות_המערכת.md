# מדריך השפה של קלוד: איך להפעיל את הכלים המחוברים אצלו בדיוק בשמות שהוא מזהה

---

### 🏛️ 1. מדוע קלוד ענה כך:
קלוד מחובר לשרת ה-MCP הראשי של Legalix שמכיל את כלי הליבה של מערכת התיקים:
* משפחת `legalix_case` (ניהול התיק, קבצים ו-OCR)
* משפחת `legalix_kepa_*` (מחסן העובדות: ישויות, ציר זמן, סתירות וראיות)
* משפחת הפרוטוקולים (`legalix_machsanim_protocol`, `legalix_analistim_protocol`, `legalix_kaftorim_protocol`)

קלוד מכיר את המערכת בדיוק דרך השמות האלה (משפחת `kepa` והפרוטוקולים של המחסנים), ולא דרך שם המיתוג השיווקי "mega case".

---

### 💬 2. מה להקליד לקלוד מעכשיו (הנוסח המדויק שמפעיל את כל הכלים שלו):

> "הפעל את הכלים של `legalix_case` ו-`kepa` עבור תיק אגרובנק / פולינר (ת״א 62449-03-24):
> 1. משוך דרך `legalix_kepa_timeline` את ציר הזמן הכרונולוגי המלא של התיק.
> 2. הפעל את `legalix_kepa_search_facts` ו-`legalix_analistim_protocol` כדי לאתר את הסתירות בין דוח בורות הגישוש של ד״ר ארילון (עמ' 9) לבין חוות דעת המהנדס שירן (סעיף 5.3.6).
> 3. חלץ דרך `legalix_kepa_get_chunk` את מראי המקום המדויקים (עמוד ושורה) של בורות 2, 3, 7 ו-8."
EOF
