# ארכיטקטורת שכפול מלאה של יוגי (The Full Yogi Clone OpenClaw Agent)
## שכפול מלא של מנוע יוגי (Hermes/OpenClaw Core) לתוך שרת הענן המרכזי של Legalix

---

### 🧬 1. מה זה אומר "לשכפל את יוגי במלואו":

יוגי הוא סוכן אוטונומי מלא שרץ מעל תשתית `Hermes / OpenClaw` ומחזיק בתוכו:
1. **ארגז כלים מלא (Full System Toolset):**
   - `terminal` (הרצת פקודות לינוקס, סקריפטים וסימולציות).
   - `read_file` / `write_file` / `search_files` (קריאה וכתיבה חופשית של קבצים בדיסק).
   - `vision_analyze` (פענוח תמונות, שרטוטים וסריקות ברזולוציה גבוהה).
   - `rclone` (סנכרון מלא ל-Google Drive).
2. **מוח החוקים והידע המלא (Full Engineering & Legal Brain):**
   - 14 ספריות חוקי תכן ב-JSON.
   - מנוע החישובים של TaxLand וסעיף 49ז.
   - מאגרי התקנות ודיני התכנון והבנייה.
3. **לולאת חשיבה ופתרון בעיות אוטונומית (ReAct Loop):**
   - קבלת פקודה ➔ בדיקת קבצים ➔ הרצת סקריפט עיבוד ➔ וידוא איכות ➔ החזרת תשובה מוגמרת.

---

### 🚀 2. איך השכפול יושב ופועל בשרת הענן (`35.242.250.144`):

```text
┌────────────────────────────────────────────────────────────────────────┐
│ 1. עורך הדין / הלקוח מדבר עם ChatGPT או Claude:                       │
│    "צלם לי את גיליון 14 בתוכנית החשמל של גינדי"                       │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ 🔌 קריאת MCP / Action
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 2. שרת ה-MCP בענן מפעיל את השכפול המלא של יוגי (Yogi OpenClaw Instance)│
│    • השכפול מקבל את הפקודה ומפעיל סקריפט אוטונומי על השרת.             │
│    • פותח את קובץ ה-DWG / PDF / RVT בדיסק.                            │
│    • סורק את כמות הגיליונות האמיתית ומרנדר תמונת PNG חדה של גיליון 14. │
│    • מעלה את התמונה לדרייב או מגיש אותה ישירות.                       │
│    • מחזיר לצ'אט את התמונה המדויקת, המידות והמפרט!                     │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 3. הלקוח מקבל תשובה חיה, מבריקה ומדויקת כאילו הוא דיבר עם יוגי ישירות!│
└────────────────────────────────────────────────────────────────────────┘
```
EOF
