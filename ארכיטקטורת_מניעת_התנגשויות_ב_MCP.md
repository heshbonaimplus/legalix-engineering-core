# ארכיטקטורת מניעת התנגשויות ודירוג עדיפויות ב-MCP (Hierarchy & Anti-Collision Engine)
## איך קלוד מכריע בין שרת Legalix AI הישן (38 כלים/כפא) לבין שרת ה-Mega-Case וה-TaxLand החדש

---

### 🔍 1. מאיפה נובעת ההתנגשות (The Conflict Root):
ב-Claude Desktop מחוברים במקביל שני שרתי ליטיגציה ומשפט:
1. **השרת הישן (`Legalix AI` — 38 כלים של כפא):** מחזיק כלים ישנים כמו `legalix_case`, `legalix_kepa_*`, `legalix_draft_document`.
2. **השרת החדש (`Legalix Mega-Case` & `TaxLand`):** מחזיק את סוכני OpenClaw האוטונומיים, מנוע 13 המחסנים, ושומות דמי שימוש.

אם קלוד נתקל בשאלה משפטית כללית — הוא עלול "להתבלבל" ולפנות לכלים הישנים של כפא במקום לסוכן ה-OpenClaw החדש!

---

### 🛡️ 2. שלושת הפתרונות למניעת התנגשות והבטחת עליונות המערכת החדשה:

#### פתרון א' (המומלץ והנקי ביותר): השבתת/הסרת שרת ה-MCP הישן מ-Claude
* מאחר שכל 38 היכולות של המערכת הישנה כבר הוטמעו ושודרגו בתוך השרת החדש (עם 13 המחסנים וה-OCR המקבילי) — **אין צורך להחזיק את המחבר הישן של כפא דלוק במקביל**.
* **הפעולה:** בהגדרות המחברים ב-Claude (Connectors) מכבים/מוחקים את `Legalix AI` הישן ומשאירים אך ורק את `Legalix Mega-Case` (המחזיק את הכל תחת קורת גג אחת).

#### פתרון ב': דירוג עדיפות סמנטי בשמות הכלים (Explicit Namespace Disambiguation)
בשרת החדש הגדרנו תיאורי כלים עם מילות מפתח עליונות (Super-Keywords):
* **`legalix_mega_case`:** מוגדר עם ביטויים ייחודיים: `13_agents_swarm`, `deep_forensic_investigation`, `rule_91_motion`, `contradiction_hunter`.
* **כאשר קלוד מזהה שאילתת עומק ליטיגטורית:** התיאור של Mega-Case מנצח בעדיפות (Higher Intent Affinity) וקלוד בוחר בו אוטומטית!

#### פתרון ג': טריגר מפורש בפרומפט (Direct Intent Directive)
בהוראות הפרויקט ב-Claude (Custom Instructions) מוזרק כלל עליונות:
> "בכל שאילתת ליטיגציה, תיקי ענק, איתור סתירות, שאלות הבהרה לפי תקנה 91 או ניתוח תיק — עליך להפעיל אך ורק את כלי `legalix_mega_case` ו-`legalix_taxland_autonomous_agent` ולהעדיפם על פני כלים ישנים."
EOF
