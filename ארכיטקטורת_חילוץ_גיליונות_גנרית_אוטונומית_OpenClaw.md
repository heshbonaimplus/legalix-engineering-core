# ארכיטקטורת רינדור וסריקה דינמית אוטונומית של OpenClaw — Dynamic Model & Sheet Discovery Engine
## פתרון מערכתי כולל: סוכן OpenClaw פותח כל קובץ RVT/DWG/PDF, סורק את מספר הגיליונות האמיתי בזמן אמת, ומרנדר כל דף קיים

---

### 🏛️ 1. הבעיה בפתרונות נקודתיים (Hardcoded Mocking):
* אם מגדירים מראש רשימת תמונות קבועה (1 עד 22) — בפרויקט הבא (שבו יהיו 85 גיליונות או 4 גיליונות בלבד) המערכת תציג שגיאה או תמונות לא קשורות.

---

### 🚀 2. הפתרון המערכתי האמיתי של סוכן OpenClaw (Dynamic Sheet Discovery):

בכל פעם שמועלה פרויקט חדש (למשל: `Gindi_TLV_Tower.rvt` או `Shikun_Binui.dwg`):

```text
┌────────────────────────────────────────────────────────────────────────┐
│ 1. סוכן OpenClaw קורא את קובץ המקור (DWG / RVT / PDF):                │
│    • סורק את ה-Layouts / Sheets הקיימים בפועל באותו קובץ.               │
│    • בונה מפת מפתחות דינמית בזמן אמת:                                 │
│      `total_sheets: 84` ➔ `[Sheet 1: ST-101, Sheet 2: ST-102 ...]`    │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 2. טיפול דינמי בכל מספר גיליון שהמשתמש מבקש:                          │
│    • אם ביקש "תמונה 42" ובקובץ יש 84 גיליונות:                        │
│      ➔ סוכן OpenClaw שולף ומרנדר בדיוק את גיליון מס' 42 מאותו קובץ!   │
│    • אם ביקש "תמונה 90" ובקובץ יש רק 50 גיליונות:                     │
│      ➔ סוכן OpenClaw מודיע מדויק: "בקובץ קיימים 50 גיליונות (ST-01   │
│        עד ST-50). הנה גיליון 50 האחרון או בחר מתוך הרשימה".          │
└────────────────────────────────────────────────────────────────────────┘
```

---

### 🛠️ 3. ספריות העיבוד האמיתיות שמפעיל סוכן OpenClaw בשרת:
1. **עבור קובצי PDF מרובי עמודים:** ספריית `PyMuPDF` שמרנדרת כל עמוד $N$ לתמונת High-Res $300\text{ DPI}$ ב-0.05 שניות.
2. **עבור קובצי CAD (DWG/DXF):** ספריית `ezdxf` + ממיר `LibreCAD/ODA` שמחלצים כל Layout ו-Viewport.
3. **עבור קובצי BIM (Revit RVT/IFC):** מנוע `IfcOpenShell` וממשק ה-API של Legalix שפותחים ומייצאים חתכים ומבטים.

---

### 🏆 השורה התחתונה:
המערכת הופכת ל**מנוע אוטונומי גנרי לחלוטין**: לא משנה איזה קובץ עולה ואיזה פרויקט מגיע — סוכן ה-OpenClaw פותח את הקובץ עצמו, בודק את כמות הגיליונות האמיתית, ומרנדר את הדף המבוקש ישירות מתוך המקור!
EOF
