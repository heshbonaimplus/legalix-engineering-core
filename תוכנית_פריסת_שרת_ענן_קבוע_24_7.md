# Legalix Engineering — Production Cloud Server Deployment Plan (24/7 Cloud Host)
# תוכנית פריסת שרת ענן קבוע ועצמאי ללא תלות במחשב המקומי (pc-ai)

## 🏛️ 1. מטרת הפריסה:
להרים שרת ענן ייעודי של Legalix שרץ 24/7/365, מכיל את כל ספריות ה-BIM, מנוע הפיזיקה (OpenSees), ספריות הכללים ב-JSON, ומשרת את ה-API של ChatGPT, Claude ורוויט גם כשהמחשב הפיזי שלך בבית כבוי לחלוטין.

---

## ☁️ 2. בחירת ספק הענן המומלץ:
יש לנו 3 אפשרויות מובילות:

### אפשרות א': Hetzner Cloud (המומלצת ביותר — חזקה, יציבה וסופר-משתלמת)
* **מפרט:** שרת CPX31 (4 Dedicated vCPUs, 8GB RAM, 160GB NVMe SSD, רוחב פס 20TB).
* **עלות:** כ-13.00 אירו לחודש (~55 ₪ בלבד).
* **יתרון:** שרת פרטי ייעודי חזק, יציבות 99.99%, מנוע לינוקס מלא ללא הגבלות.

### אפשרות ב': Google Cloud Platform (GCP Compute Engine / Cloud Run)
* **מפרט:** מכונת e2-standard-4 (4 vCPUs, 16GB RAM).
* **עלות:** כ-40$–60$ לחודש.
* **יתרון:** חיבור ישיר ל-Google Drive ו-Google Cloud Storage תחת אותו חשבון Gmail.

### אפשרות ג': DigitalOcean Droplet
* **מפרט:** Droplet 8GB RAM, 4 vCPUs.
* **עלות:** כ-40$ לחודש.

---

## 🚀 3. תהליך הפריסה (ב-4 פקודות אוטומטיות שיוגי מריץ):

ברגע שיש את פרטי השרת (IP וסיסמה):
1. **שכפול הקוד מ-GitHub:**
   `git clone https://github.com/heshbonaimplus/legalix-engineering-core.git /opt/legalix`
2. **התקנת סביבת הפיזיקה וה-BIM:**
   `pip install openseespy scipy numpy docx mcp`
3. **הגדרת שירות Systemd קבוע (Auto-Start on Boot):**
   הגדרת שירות `legalix-api.service` שרץ ברקע תמיד ומתאושש אוטומטית מכל נפילה.
4. **חיבור דומיין מאובטח ב-SSL/HTTPS:**
   חיבור דומיין רשמי (למשל `api.legalix.io` או Cloudflare Dedicated Tunnel).

---

## 🎯 4. התוצאה הסופית:
* ה-ChatGPT והטלפון שלך מחוברים ישירות ל-`https://api.legalix.io/v1`.
* **אפס תלות במחשב המקומי שלך (אתה יכול לכבות אותו, לסגור את המסך, או לטוס לחו\"ל).**
* המערכת עונה, מחשבת ומפיקה דוחות 24/7 ללא הפסקה!
EOF
