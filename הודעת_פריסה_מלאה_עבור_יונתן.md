# הודעת פריסה והנחיות טכניות עבור יונתן (Production Deployment Guide)
## נושא: פריסת שרת ה-MCP המאוחד הקבוע ב-Cloud Run עבור Claude & ChatGPT (ללא מנהרות זמניות)

---

היי יונתן, שבוע טוב!

אנחנו צריכים לחבר את שרת ה-MCP הרשמי הקבוע של **Legalix** בענן של גוגל (`https://mcp.legalix.ai/mcp`) כדי שקלוד ו-ChatGPT יתחברו אליו קבוע ללא שום מנהרות זמניות.

השרת המאוחד מרכז תחתיו את כל 3 המודולים:
1. 🏗️ **Legalix Engineering** (בקרת תכן ל-14 היועצים, שרטוטי CAD/BIM, רינדור גיליונות וכתבי כמויות BOQ).
2. ⚖️ **Legalix Mega-Case** (חדר מלחמה ליטיגטורי וסריקת 13 המחסנים בדרייב).
3. 🏛️ **Legalix TaxLand** (מנוע חישוב מס מקרקעין, מס שבח ליניארי מוטב וסעיף 49ז).

---

### 🐙 1. מאגר הקוד המעודכן ב-GitHub:
כל הקוד, המנועים, הסולברים ושרת ה-MCP המאוחד ארוזים ומסונכרנים במאגר:  
👉 **`https://github.com/heshbonaimplus/legalix-engineering-core`**  
*(Branch: `main`)*

---

### 🚀 2. פקודת הפריסה ב-Google Cloud Run (שתי פקודות בלבד):

בטרמינל / Cloud Shell של פרויקט ה-GCP שלכם, הרץ את שתי הפקודות הבאות:

```bash
# 1. משיכת הקוד המעודכן
git clone https://github.com/heshbonaimplus/legalix-engineering-core.git /tmp/legalix-core
cd /tmp/legalix-core

# 2. בנייה ופריסה ישירה ל-Cloud Run תחת הדומיין הרשמי
gcloud run deploy legalix-master-mcp \
  --source . \
  --region europe-west3 \
  --platform managed \
  --allow-unauthenticated \
  --port 8080 \
  --memory 2Gi \
  --cpu 2
```

---

### 🌐 3. קישור הדומיין הרשמי:
ודא ששירות ה-Cloud Run ממופה לכתובת הקבועה:  
👉 **`https://mcp.legalix.ai/mcp`**

---

### 🏆 מה נקבל בסיום הפריסה:
1. **חיבור קבוע לקלוד (Claude MCP):** כל משתמש מוסיף בהגדרות ה-Connectors את `https://mcp.legalix.ai/mcp` וכל 3 המודולים זמינים לו 24/7 ללא שום שגיאת Billing.
2. **חיבור קבוע ל-ChatGPT Actions:** ה-Action מצביע על הדומיין הרשמי ללא שום צורך במנהרות זמניות של Cloudflare.
3. **אפס תלות במחשב מקומי:** המערכת רצה עצמאית בענן המאובטח של גוגל!

תודה רבה!
EOF
