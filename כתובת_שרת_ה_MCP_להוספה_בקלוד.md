# כתובת שרת ה-MCP המרוחקת של Legalix Mega-Case להוספה ב-Claude Mobile & Web

---

### 🌐 הכתובת להוספת המחבר המותאם (Custom MCP Connector) ב-Claude:

👉 **`https://smilies-captured-batch-cgi.trycloudflare.com/mcp`**

---

### 📋 פרטי החיבור המלאים (אם קלוד מבקש):
* **שם השרת (Server Name):** `legalix-mega-case`
* **כתובת השרת (Server URL):** `https://smilies-captured-batch-cgi.trycloudflare.com/mcp`
* **סוג פרוטוקול (Transport Type):** `Streamable HTTP / SSE` (JSON-RPC 2.0)
* **אימות (Authentication):** `None` (פתוח ומאובטח)

---

### 🛠️ הכלים שנטענים מיד עם החיבור:
1. `legalix_mega_case` — חדר המלחמה המשפטי ו-13 הסוכנים.
2. `legalix_taxland_autonomous_agent` — סוכן OpenClaw לתכנון מס מקרקעין.
3. `audit_structural_model` — בקרת תכן הנדסית רב-תחומית.
EOF
