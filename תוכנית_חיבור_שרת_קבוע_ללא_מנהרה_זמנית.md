# תוכנית פריסת שרת ענן קבוע ורשמי (Production Static Host) עבור Legalix Suite
# פתרון קבוע ללא מנהרות זמניות — מחובר 24/7/365 ב-Google Cloud (GCP)

---

### 🌐 1. פרטי שרת הענן הקיים והקבוע שלך:
שרת הענן המרכזי של Legalix ב-Google Cloud:
* **שם השרת:** `legalix-master-server`
* **אזור:** `europe-west3-a (Frankfurt)`
* **כתובת IP קבועה (Static External IP):** 👉 **`35.242.250.144`**

---

### 🏛️ 2. שלוש הדרכים לקביעת כתובת MCP קבועה לעד:

#### אפשרות א': חיבור ישיר ל-IP הקבוע בפורט 80 (הכי מהיר — ללא צורך בדומיין):
* השרת מאזין ישירות לכתובת האינטרנט הקבועה שלו:
  👉 `http://35.242.250.144/mcp`
* **יתרון:** קבוע לתמיד, לעולם לא משתנה, ולא תלוי באף מנהרה זמנית!

#### אפשרות ב': חיבור דומיין רשמי ממותג עם תעודת SSL מאובטחת (המומלץ ביותר למכירה ללקוחות):
1. **הגדרת רשומת A ב-DNS:**  
   הדומיין שלך (למשל: `api.legalix.ai` או `mcp.legalix.ai`) מפנה ל-IP: `35.242.250.144`.
2. **הפעלת תעודת SSL / HTTPS מאובטחת (Certbot Let's Encrypt ע״י יוגי בשרת).**
3. **כתובות ה-MCP הקבועות שיווצרו:**
   * 🏗️ הנדסה: `https://api.legalix.ai/mcp/engineering`
   * 🏛️ מיסוי: `https://api.legalix.ai/mcp/taxland`
   * ⚖️ ליטיגציה: `https://api.legalix.ai/mcp/megacase`

#### אפשרות ג': Cloudflare Tunnel קבוע מבוסס Token (Named Cloudflare Tunnel):
* במקום מנהרה אקראית זמנית (`trycloudflare`), מגדירים מנהרה קבועה של החשבון שלך ב-Cloudflare (`cloudflared service install <TOKEN>`).
* המנהרה נשארת עם אותו שם דומיין קבוע לעד גם אם השרת מופעל מחדש!
EOF
