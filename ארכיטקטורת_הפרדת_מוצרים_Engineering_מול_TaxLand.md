# ארכיטקטורת הפרדת מוצרים ומסלולים מסחריים — Legalix Unified Brain vs Product Gateways
# חלוקה ברורה בין Legalix Engineering לבין Legalix TaxLand עם מוח מקצועי מאוחד מאחורי הקלעים

## 🏛️ 1. הארכיטקטורה הכפולה (The Dual-Product Gateway Architecture):

מאחורי הקלעים — המוח של Legalix מאוחד ומחזיק את כל הספרות, הפסיקה, החקיקה, התקנים וההנדסה.
מלפנים — הלקוח רואה אך ורק את המוצר הייעודי שהוא שילם עליו ברישיון:

```text
                                  🧠 מוח הידע המאוחד של Legalix
                    (חקיקה + פסיקה + נמדר + דויטש + הספר הכחול + תקנים + OpenSees)
                                               │
                    ┌──────────────────────────┴──────────────────────────┐
                    ▼                                                     ▼
     🏗️ מוצר א׳: Legalix Engineering                       🏛️ מוצר ב׳: Legalix TaxLand
  ┌─────────────────────────────────────┐               ┌─────────────────────────────────────┐
  │ • למי מיועד: יזמים, קבלנים,        │               │ • למי מיועד: עורכי דין מקרקעין,     │
  │   מהנדסי שלד, אנשי BIM ויועצי MEP. │               │   רואי חשבון, יועצי מס, שמאים ויזמים│
  │ • מה הלקוח רואה בצ'אט/רוויט:        │               │ • מה הלקוח רואה בצ'אט/וורד:        │
  │   - בקרת תכן וסופרפוזיציה.          │               │   - תכנון מס שבח ליניארי מוטב.      │
  │   - הצלבות מכר מול ביצוע.           │               │   - פיצול רעיוני זכויות בנייה (49ז). │
  │   - אנליזות פיזיקה ושקיעות קרקע.    │               │   - מדרגות מס רכישה 2026.           │
  │   - תוסף רוויט וקובצי BCF.          │               │   - פטור דירת מגורים וירושה.        │
  │ • חסימת תוכן: לא רואה מיסוי ומשפט!   │               │ • חסימת תוכן: לא רואה BIM ושלד!     │
  └─────────────────────────────────────┘               └─────────────────────────────────────┘
```

---

## 🔒 2. איך מתבצעת ההפרדה המוחלטת מול הלקוחות:

1. **קישורי GPT וסוכנים נפרדים:**
   * לקוח הנדסה מקבל קישור רק ל-**`Legalix Engineering Co-Pilot`**.
   * לקוח מיסוי מקרקעין מקבל קישור רק ל-**`Legalix TaxLand Co-Pilot`**.
2. **מפתחות רישיון נפרדים (API License Keys):**
   * רישיון הנדסה (`LIC-ENG-XXXX`) פותח גישה רק לפונקציות ה-BIM, השרטוטים וה-FEA.
   * רישיון מיסוי (`LIC-TAX-YYYY`) פותח גישה רק למנוע ה-Numeric Core של מס שבח, רכישה והשבחה.
3. **אפס בלבול וזליגת תחומים:**
   * עורך דין שמשתמש ב-TaxLand לא מקבל תשובות על קורות בטון וספרינקלרים.
   * מהנדס בניין שמשתמש ב-Engineering לא מקבל תשובות על סעיף 49ב(2) ושומה עצמית.

---

## 💡 3. היתרון הענק עבור אלי (The Master Admin Advantage):
* **לך (עו״ד אלי עמר):** יש גישת-על (Master Key) לשני העולמות במקביל.
* **הצלבה ייחודית בעסקאות נדל״ן מורכבות:** אתה יכול לקחת פרויקט (כמו לוד ניר צבי) ולנתח אותו בו-זמנית:
  * **בהיבט ההנדסי:** זכויות בנייה, שלד, התאמת מכר וסופרפוזיציה.
  * **בהיבט המיסויי:** חשיפת מס שבח, היטל השבחה מול הוועדה המקומית ותכנון מס אופטימלי ליזם!
EOF
